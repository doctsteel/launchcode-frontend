import { Button, Container } from "@chakra-ui/react";
import React from "react";

const QuoteDetailsButton = (children: any): React.PropsWithChildren<any> => {
  return (
    <Container>
      <Button>details</Button>
    </Container>
  );
};

export default QuoteDetailsButton;
